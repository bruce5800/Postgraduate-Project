%% LOCAL STUB for hyperxmp (XMP metadata embedding). The real package is not
%% user-mode installable on BasicTeX ("not relocatable"). acmart loads it only to
%% embed XMP metadata in the final PDF, which drafts do not need. Remove this
%% stub (and install the real hyperxmp via a full TeX Live) for the camera-ready.
\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{hyperxmp}[2026/07/28 v0.0 local no-op stub]
\endinput
